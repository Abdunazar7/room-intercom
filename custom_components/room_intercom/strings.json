{
  "config": {
    "step": {
      "user": {
        "title": "Room Intercom",
        "description": "Set up Room Intercom. There is nothing to configure — pick your speakers later in the Room Intercom card."
      }
    },
    "abort": {
      "already_configured": "Room Intercom is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Room Intercom",
        "description": "Choose which speakers appear in the Intercom panel. Leave empty to show all media players that support play media. The browser microphone needs HTTPS — Room Intercom can serve a local HTTPS port itself (reused automatically if another intercom already serves it).",
        "data": {
          "speakers": "Intercom speakers (leave empty for all)",
          "enable_https": "Serve a built-in HTTPS port for the microphone",
          "proxy_port": "HTTPS port"
        }
      }
    }
  }
}
